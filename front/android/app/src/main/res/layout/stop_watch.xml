<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_idog_widget"
    android:visibility="visible"
    android:orientation="horizontal"
    android:padding="16dp">

    <!-- leftW (Profile) -->
    <ImageView
        android:id="@+id/profile"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:src="@drawable/vaccine_icon"
        android:adjustViewBounds="true"
        android:scaleType="centerInside"
        />


    <!-- rightW -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:orientation="vertical"
        android:gravity="center">

        <!-- rightW_upH (Buttons) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:gravity="center"
            android:orientation="horizontal"
            android:layout_marginBottom="8dp">

            <!-- Play or Stop Button based on isRunning -->
            <Button
                android:id="@+id/playButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:background="@drawable/possible_button"
                android:text="재생"
                android:layout_marginRight="4dp" />
            <Button
                android:id="@+id/stopButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:background="@drawable/possible_button"
                android:text="정지"
                android:layout_marginRight="4dp"
                android:visibility="gone"/>
            <Button
                android:id="@+id/resetButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:background="@drawable/impossible_button"
                android:text="초기화"
                android:layout_marginLeft="4dp" />

        </LinearLayout>

        <!-- rightW_downH (Timer) -->
        <TextView
            android:id="@+id/timer"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="@drawable/background_timer"
            android:gravity="center"
            android:text="00:00:00"
            android:textSize="18sp"
            android:layout_margin="2dp"/>

    </LinearLayout>
</LinearLayout>
